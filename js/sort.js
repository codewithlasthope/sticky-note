const Sort = {

    sort(notes, type) {

        const sorted = [...notes];

        switch(type) {

            case "updated":
                break;

            case "created":
                break;

            case "title":
                break;

            default:
                return sorted;
        }

    }

}